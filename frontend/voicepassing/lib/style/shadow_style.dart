import 'package:flutter/material.dart';

class ShadowStyle {
  static BoxShadow wideShadow = const BoxShadow(
    color: Color.fromARGB(30, 0, 0, 0),
    spreadRadius: 2,
    blurRadius: 15,
    offset: Offset(0, 4),
  );
}
